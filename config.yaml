# ─────────────────────────────────────────────────
# Synapse – Central Configuration
# ─────────────────────────────────────────────────

synapse:
  host: "0.0.0.0"
  backend_port: 8000
  frontend_port: 5173

# ── Managed Services ─────────────────────────────
# Add services / databases / companion apps here.
# Each entry will appear in the Synapse Services page.
#
# Example:
#   - name: "Cloud Storage Uploader"
#     url: "http://192.168.1.112:9000"
#     health_check: "/health"
#     description: "Uploads photos to cloud storage"
#
services: []
